import RpButtonGroup from '../button/src/button-group'

/* istanbul ignore next */
RpButtonGroup.install = function(Vue) {
    Vue.component(RpButtonGroup.name, RpButtonGroup)
}

export default RpButtonGroup
